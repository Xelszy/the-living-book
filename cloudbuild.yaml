steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '--build-arg', 'API_KEY=${_API_KEY}',
      '-t', 'gcr.io/$PROJECT_ID/the-living-book:latest',
      '.'
    ]
images:
  - 'gcr.io/$PROJECT_ID/the-living-book:latest'
